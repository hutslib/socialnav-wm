# @package _global_
# 实验 6: Pretrained WM + ratio 0.0625 (每 16 次 PPO 做 1 次 WM)

defaults:
  - exp_02_full_wm
  - _self_

habitat_baselines:
  tensorboard_dir: "experiments/exp_06_pretrain_ratio_006/tb"
  video_dir: "experiments/exp_06_pretrain_ratio_006/video"
  checkpoint_folder: "experiments/exp_06_pretrain_ratio_006/checkpoints"

  world_model:
    wm_train_ratio: 0.0625
    wm_epochs_per_update: 240
    pretrained_wm_checkpoint: "experiments/exp_02_full_wm/checkpoints/latest.pth"
