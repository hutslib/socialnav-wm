# @package _global_
# 实验 7: ratio 对比 - 每 16 次 PPO 做 1 次 WM (interval=16, ratio=0.0625)

defaults:
  - exp_02_full_wm
  - _self_

habitat_baselines:
  tensorboard_dir: "experiments/exp_07_ratio_016/tb"
  video_dir: "experiments/exp_07_ratio_016/video"
  checkpoint_folder: "experiments/exp_07_ratio_016/checkpoints"

  world_model:
    wm_train_ratio: 0.0625
    wm_warmup_updates: 2
    wm_grad_clip: 100.0
    wm_epochs_per_update: 240
