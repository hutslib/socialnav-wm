# @package _global_
# 实验 9: falcon baseline no aux (Baseline 无辅助损失)

defaults:
  - exp_01_baseline_falcon
  - _self_

habitat_baselines:
  tensorboard_dir: "experiments/exp_09_baseline_no_aux/tb"
  video_dir: "experiments/exp_09_baseline_no_aux/video"
  checkpoint_folder: "experiments/exp_09_baseline_no_aux/checkpoints"

  rl:
    auxiliary_losses: {}  # 无辅助损失（schema 要求 Dict，不可用 null）
