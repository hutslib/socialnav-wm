# @package _global_
# 实验 6: ratio 对比 - 每 4 次 PPO 做 1 次 WM (interval=4, ratio=0.25)

defaults:
  - exp_02_full_wm
  - _self_

habitat_baselines:
  tensorboard_dir: "experiments/exp_06_ratio_004/tb"
  video_dir: "experiments/exp_06_ratio_004/video"
  checkpoint_folder: "experiments/exp_06_ratio_004/checkpoints"

  world_model:
    wm_train_ratio: 0.25
    wm_warmup_updates: 2
    wm_grad_clip: 100.0
    wm_epochs_per_update: 60
